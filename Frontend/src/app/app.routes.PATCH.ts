// ============================================================
// DEV 3 — ADD THIS ROUTE to app.routes.ts (team lead merges)
// ============================================================
// Add inside the routes array in src/app/app.routes.ts:

/*
  {
    path: 'matches',
    loadComponent: () => import('./components/matches/matches').then(m => m.MatchesComponent),
    canActivate: [authGuard]
  },
*/

// Also add "Matches" link to header.html nav:
/*
  <a routerLink="/matches" routerLinkActive="active" class="nav-link">Matches</a>
*/
